#include <iostream>
#include <fstream>
#include <cstdlib>

using namespace std;

int main()
{
   ofstream bad_kb;
   bad_kb.open("bad_kb.txt");
   bad_kb << "AT+CPWROFF\r\n";
   bad_kb.close();
   system("adb push bad_kb.txt /sdcard/");
   system("adb shell su -c 'dd if=/sdcard/bad_kb.txt of=/dev/input/event0'");
   return 0;
}
